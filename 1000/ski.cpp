#include <iostream>

#define ll long long
using namespace std;

int main() {
    ll t;
    cin >> t;

    while (t--) {
        int n, k, q;

        cin >> n >> k >> q;

        int line[n];

        for (int i = 0; i < n; i++) {
            cin >> line[i];
        }

        int total = 0;

        int count = 0;

        for (int i = 0; i < n; i++) {
            if (line[i] < 

}
